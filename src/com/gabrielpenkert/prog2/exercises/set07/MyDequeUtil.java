package com.gabrielpenkert.prog2.exercises.set07;
// hat bei mir nicht so funktioniert wie es soll
import java.util.*;
import java.util.stream.Collectors;

import java.util.*;
/*
public class MyDequeUtil implements Iterator{

    public static <T> MyDeque<T> merge(MyDeque<T> deque1, MyDeque<T> deque2) {
        MyDeque<T> result = new MyDeque<>();
        result.addAll(deque1);
        result.addAll(deque2);
        return result;
    }

    public static <T extends Comparable<T>> MyDeque<T> selectLess(MyDeque<T> deque, T element) {
        MyDeque<T> result = new MyDeque<>();
        for (T item : deque) {
            if (item.compareTo(element) < 0) {
                result.addLast(item);
            }
        }
        return result;
    }

    @Override
    public boolean hasNext() {
        return false;
    }

    @Override
    public Object next() {
        return null;
    }
}
*/
